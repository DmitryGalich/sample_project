
services:
   gateway:
    image: nginx:1.27
    restart: unless-stopped
    container_name: gateway
    ports:
      - "8080:80"
    networks:
      - internal_network
    volumes:
      - ./gateway/nginx.conf:/etc/nginx/nginx.conf:ro

networks:
  internal_network:
    driver: bridge