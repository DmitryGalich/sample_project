events {
    worker_connections 1024;
}

http {
    access_log /var/log/nginx/access.log;
    
    server {
        listen 8080;
        http2 on; 

        server_name localhost;

        location / {
            return 404;
        } 

        location /test {
            return 200 "test";
        } 

        location /service_users/ {
            proxy_pass http://service_users:50051/;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }

        # location /messenger.MessengerCoreService/ {
        #     grpc_pass grpc://service_users:50051;
            
        #     grpc_read_timeout 86400s;
        #     grpc_send_timeout 86400s;
        # }
    }

    # upstream messenger_backend {
    #     server messenger:50051;
    # }

    # server {
    #     listen 8080;
    #     http2 on; 

    #     server_name localhost;

    #     location /messenger.MessengerCoreService/ {
    #         grpc_pass grpc://messenger_backend;
            
    #         grpc_read_timeout 86400s;
    #         grpc_send_timeout 86400s;
    #     }

    #     location / {
    #         return 404;
    #     }
    # }
}
