{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug users",
            "type": "lldb",
            "request": "launch",
            "cargo": {
                "args": [
                    "build"
                ],
                "filter": {
                    "kind": "bin"
                },
                "cwd": "${workspaceFolder}/users"
            },
            "args": [],
            "cwd": "${workspaceFolder}/users"
        }
    ]
}
